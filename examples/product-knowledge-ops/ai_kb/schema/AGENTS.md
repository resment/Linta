# Linta Maintenance Rules

This example follows the same rules as generated knowledge bases:

- never edit `ai_kb/raw/`;
- use source cards before raw when possible;
- write proposed current-state changes to `current_draft/`;
- write `current/` only after explicit human confirmation;
- cite source paths for important claims.
